export enum GENDER_CHOICES {
    male = 'male',
    female = 'female',
}

export enum MARITAL_STATUS_CHOICES {
    single = 'single',
    married = 'married',
    divorced = 'divorced',
    widowed = 'widowed',
}

export enum CONTRACT_TYPE_CHOICES {
    CDI = 'CDI',
    CDD = 'CDD',
    stage = 'stage',
    consultant = 'consultant',
}

export enum LEVEL_SKILL_CHOICES {
    beginner = 'beginner',
    intermediate = 'intermediate',
    advanced = 'advanced',
    expert = 'expert',
}

export enum STATUS_CHOICES {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
    CANCELED = 'CANCELED',
}

export enum PERIOD_CHOICES {
    AM = 'AM',
    PM = 'PM',
}